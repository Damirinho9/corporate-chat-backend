# Corporate Chat Backend

Полноценный backend для корпоративного чата с иерархической системой доступа, real-time коммуникацией, системой поддержки, видеозвонками и веб-уведомлениями.

> 📚 **Документация**: Проект следует [AI Dev 2025 протоколу](CLAUDE.md). Перед началом работы прочитайте:
> - [docs/architecture.md](docs/architecture.md) — архитектура и ключевые модули
> - [docs/ai-coding.md](docs/ai-coding.md) — правила разработки
> - [docs/runbooks/common-issues.md](docs/runbooks/common-issues.md) — типичные проблемы

## 🚀 Основные возможности

### ✅ Реализовано (Production-ready)

#### 1. Backend и База данных
- ✅ Node.js + Express.js сервер
- ✅ PostgreSQL база данных с оптимизированной схемой
- ✅ Пул соединений для производительности
- ✅ Транзакции для целостности данных
- ✅ Индексы для быстрых запросов

#### 2. Безопасность
- ✅ Хеширование паролей (bcrypt с 12 раундами)
- ✅ JWT токены для аутентификации
- ✅ Refresh tokens для обновления сессий
- ✅ Helmet для защиты HTTP заголовков
- ✅ CORS настроен правильно
- ✅ Rate limiting (защита от спама и брутфорса)
- ✅ Валидация всех входных данных
- ✅ Защита от SQL-инъекций (parameterized queries)
- ✅ Защита от XSS атак
- ✅ Сессии с автоматическим таймаутом

#### 3. Real-time коммуникация
- ✅ WebSocket через Socket.io
- ✅ Мгновенная доставка сообщений
- ✅ Статусы "онлайн/оффлайн"
- ✅ Индикатор "печатает..."
- ✅ Автоматические уведомления о новых сообщениях
- ✅ Real-time обновление чатов

#### 4. Масштабируемость
- ✅ Пагинация сообщений (ленивая загрузка)
- ✅ Оптимизация запросов к БД
- ✅ Индексы на всех важных полях
- ✅ Эффективные JOIN запросы
- ✅ Подготовка для горизонтального масштабирования

#### 5. API Features
- ✅ RESTful API architecture
- ✅ Иерархические права доступа (5 ролей)
- ✅ CRUD операции для всех сущностей
- ✅ Поиск по сообщениям
- ✅ Статистика и аналитика
- ✅ Редактирование и удаление сообщений
- ✅ Управление пользователями
- ✅ Управление чатами

#### 6. Система поддержки (Support/Troubleshooting)
- ✅ Тикет-система с SLA мониторингом
- ✅ Приоритеты и статусы тикетов
- ✅ Автоназначение тикетов на агентов
- ✅ Команды поддержки
- ✅ История изменений статусов
- ✅ Аналитика по тикетам
- ✅ Workflow автоматизация
- ✅ Real-time уведомления по тикетам

#### 7. Web Push Уведомления
- ✅ Service Worker для push notifications
- ✅ VAPID ключи для безопасности
- ✅ Подписка/отписка от уведомлений
- ✅ Настройки уведомлений для пользователей
- ✅ Логирование отправленных уведомлений
- ✅ Push при новых сообщениях (для оффлайн пользователей)

#### 8. Видеозвонки
- ✅ Интеграция с Jitsi Meet
- ✅ История звонков
- ✅ Участники звонков
- ✅ События во время звонков
- ✅ Real-time статус звонков

#### 9. Боты и Webhooks
- ✅ API для ботов
- ✅ Управление ботами
- ✅ Webhooks для внешних интеграций
- ✅ События webhook (новые сообщения, тикеты)
- ✅ Безопасные webhook secrets

#### 10. Дополнительные функции
- ✅ Опросы в чатах
- ✅ Заявки на регистрацию с email-подтверждением
- ✅ Реакции на сообщения
- ✅ Пересылка сообщений
- ✅ Загрузка и управление файлами
- ✅ Отделы и департаменты

## 📋 Требования

- Node.js >= 14.0.0
- PostgreSQL >= 12.0
- npm или yarn

## 🛠️ Установка

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей
npm install
```

### 2. Настройка PostgreSQL

```bash
# Войти в PostgreSQL
sudo -u postgres psql

# Создать базу данных
CREATE DATABASE corporate_chat;

# Создать пользователя (опционально)
CREATE USER chat_admin WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE corporate_chat TO chat_admin;

# Выйти
\q
```

### 3. Инициализация схемы базы данных

```bash
# Выполнить SQL скрипт для создания таблиц
psql -U postgres -d corporate_chat -f database/schema.sql

# Или если создали пользователя:
psql -U chat_admin -d corporate_chat -f database/schema.sql
```

### 4. Настройка переменных окружения

```bash
# Скопировать пример конфигурации
cp .env.example .env

# Отредактировать .env и установить свои значения
nano .env
```

**Важно!** Обязательно измените следующие значения в `.env`:

```env
# Сгенерируйте надежные секретные ключи (минимум 32 символа)
JWT_SECRET=your_very_long_random_secret_key_here_min_32_chars
JWT_REFRESH_SECRET=your_another_very_long_random_key_here

# Настройте подключение к базе данных
DB_HOST=localhost
DB_PORT=5433
DB_NAME=corporate_chat
DB_USER=postgres
DB_PASSWORD=your_postgres_password

# Укажите origin вашего фронтенда
CORS_ORIGIN=http://localhost:8080

# Web Push (VAPID keys) - для push notifications
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
VAPID_SUBJECT=mailto:your-email@example.com

# Email (для уведомлений и регистрации)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-email-password

# Jitsi (опционально, если используете свой сервер)
JITSI_DOMAIN=meet.jit.si

# Окружение
NODE_ENV=development
PORT=3000
```

**Генерация секретных ключей:**

```bash
# JWT секреты (в Node.js)
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# VAPID ключи для Web Push
npx web-push generate-vapid-keys

# Или онлайн:
# https://www.random.org/strings/
```

### 5. Заполнение демо-данными

```bash
# Создать демо-пользователей и чаты
node database/seed.js
```

## 🚀 Запуск

### Development режим

```bash
npm run dev
```

Сервер запустится с автоперезагрузкой при изменении файлов (nodemon).

### Production режим

```bash
npm start
```

## 📡 API Endpoints

### Authentication

```
POST /api/auth/register       - Регистрация пользователя (admin only)
POST /api/auth/login          - Вход в систему
POST /api/auth/refresh        - Обновление токена
GET  /api/auth/profile        - Получить профиль
PUT  /api/auth/change-password - Сменить пароль
```

### Users

```
GET    /api/users                    - Все пользователи (admin)
GET    /api/users/online             - Онлайн пользователи
GET    /api/users/:id                - Пользователь по ID
GET    /api/users/stats              - Статистика пользователей (admin)
GET    /api/users/role/:role         - Пользователи по роли
GET    /api/users/department/:dept   - Пользователи по отделу
PUT    /api/users/:id                - Обновить пользователя (admin)
DELETE /api/users/:id                - Удалить пользователя (admin)
POST   /api/users/:id/reset-password - Сбросить пароль (admin)
```

### Chats

```
GET    /api/chats                - Доступные чаты
GET    /api/chats/:id            - Чат по ID
POST   /api/chats/direct         - Создать личный чат
POST   /api/chats/group          - Создать групповой чат (admin)
POST   /api/chats/:id/participants - Добавить участника (admin)
DELETE /api/chats/:id/participants/:userId - Удалить участника (admin)
PUT    /api/chats/:id/read       - Отметить как прочитанное
DELETE /api/chats/:id            - Удалить чат (admin)
```

### Messages

```
GET    /api/chats/:id/messages           - Получить сообщения (с пагинацией)
POST   /api/chats/:id/messages           - Отправить сообщение
PUT    /api/messages/:id                 - Редактировать сообщение
DELETE /api/messages/:id                 - Удалить сообщение
GET    /api/chats/:id/messages/search    - Поиск по сообщениям
GET    /api/messages/all                 - Все сообщения (admin)
GET    /api/messages/stats               - Статистика сообщений (admin)
```

### Health

```
GET /api/health - Проверка работоспособности сервера
```

### Support (Troubleshooting System)

```
# Tickets
GET    /api/support/tickets              - Все тикеты
POST   /api/support/tickets              - Создать тикет
GET    /api/support/tickets/:id          - Тикет по ID
PUT    /api/support/tickets/:id          - Обновить тикет
DELETE /api/support/tickets/:id          - Удалить тикет
PUT    /api/support/tickets/:id/assign   - Назначить агента
PUT    /api/support/tickets/:id/status   - Изменить статус
GET    /api/support/tickets/:id/history  - История изменений

# Ticket Messages
GET    /api/support/tickets/:id/messages - Сообщения тикета
POST   /api/support/tickets/:id/messages - Добавить сообщение

# Teams
GET    /api/support/teams                - Все команды поддержки
POST   /api/support/teams                - Создать команду
GET    /api/support/teams/:id            - Команда по ID
PUT    /api/support/teams/:id            - Обновить команду
DELETE /api/support/teams/:id            - Удалить команду

# Analytics
GET    /api/support/analytics/overview   - Общая аналитика
GET    /api/support/analytics/agent/:id  - Метрики агента
GET    /api/support/analytics/sla        - SLA метрики
```

### Push Notifications

```
POST   /api/push/subscribe               - Подписаться на push
POST   /api/push/unsubscribe             - Отписаться от push
GET    /api/push/settings                - Настройки уведомлений
PUT    /api/push/settings                - Обновить настройки
POST   /api/push/test                    - Тестовое уведомление
```

### Calls (Video Calls)

```
GET    /api/calls/history/all            - История звонков
GET    /api/calls/:id                    - Информация о звонке
POST   /api/calls                        - Создать звонок
PUT    /api/calls/:id/end                - Завершить звонок
POST   /api/calls/:id/participants       - Добавить участника
```

### Bots

```
GET    /api/bots                         - Все боты
POST   /api/bots                         - Создать бота
GET    /api/bots/:id                     - Бот по ID
PUT    /api/bots/:id                     - Обновить бота
DELETE /api/bots/:id                     - Удалить бота
POST   /api/bots/:id/regenerate-token    - Обновить токен бота
```

### Webhooks

```
GET    /api/webhooks                     - Все webhooks
POST   /api/webhooks                     - Создать webhook
GET    /api/webhooks/:id                 - Webhook по ID
PUT    /api/webhooks/:id                 - Обновить webhook
DELETE /api/webhooks/:id                 - Удалить webhook
POST   /api/webhooks/:id/test            - Тестовый webhook
```

### Polls

```
GET    /api/polls                        - Все опросы
POST   /api/polls                        - Создать опрос
GET    /api/polls/:id                    - Опрос по ID
POST   /api/polls/:id/vote               - Проголосовать
GET    /api/polls/:id/results            - Результаты опроса
```

### Registration

```
POST   /api/registration/request         - Заявка на регистрацию
GET    /api/registration/requests        - Все заявки (admin)
PUT    /api/registration/requests/:id/approve - Одобрить (admin)
PUT    /api/registration/requests/:id/reject  - Отклонить (admin)
```

### Departments

```
GET    /api/departments                  - Все отделы
POST   /api/departments                  - Создать отдел (admin)
GET    /api/departments/:id              - Отдел по ID
PUT    /api/departments/:id              - Обновить отдел (admin)
DELETE /api/departments/:id              - Удалить отдел (admin)
```

### Files

```
POST   /api/files/upload                 - Загрузить файл
GET    /api/files/:id                    - Скачать файл
DELETE /api/files/:id                    - Удалить файл
GET    /api/files/stats                  - Статистика файлов (admin)
```

### Analytics

```
GET    /api/analytics/messages           - Аналитика сообщений
GET    /api/analytics/users              - Аналитика пользователей
GET    /api/analytics/activity           - Активность системы
```

## 🔌 WebSocket Events

### Client -> Server

```javascript
// Подключение (требует токен)
io.connect('http://localhost:3000', {
    auth: { token: 'your_jwt_token' }
});

// События
socket.emit('send_message', { chatId, content });
socket.emit('typing_start', { chatId });
socket.emit('typing_stop', { chatId });
socket.emit('mark_as_read', { chatId });
socket.emit('edit_message', { messageId, content, chatId });
socket.emit('delete_message', { messageId, chatId });
socket.emit('join_chat', { chatId });
socket.emit('leave_chat', { chatId });
```

### Server -> Client

```javascript
// Получение событий - Чаты
socket.on('new_message', (data) => { /* новое сообщение */ });
socket.on('user_typing', (data) => { /* пользователь печатает */ });
socket.on('user_stopped_typing', (data) => { /* перестал печатать */ });
socket.on('message_edited', (data) => { /* сообщение отредактировано */ });
socket.on('message_deleted', (data) => { /* сообщение удалено */ });
socket.on('message_read', (data) => { /* прочитано */ });
socket.on('user_online', (data) => { /* пользователь онлайн */ });
socket.on('user_offline', (data) => { /* пользователь оффлайн */ });

// Получение событий - Support System
socket.on('ticket_created', (data) => { /* новый тикет */ });
socket.on('ticket_updated', (data) => { /* тикет обновлен */ });
socket.on('ticket_assigned', (data) => { /* тикет назначен */ });
socket.on('ticket_status_changed', (data) => { /* статус изменен */ });
socket.on('ticket_message', (data) => { /* новое сообщение в тикете */ });
socket.on('sla_warning', (data) => { /* предупреждение SLA */ });

// Общие события
socket.on('error', (data) => { /* ошибка */ });
```

## 🔐 Демо-пользователи

После выполнения `node database/seed.js`:

| Логин | Пароль | Роль | Отдел |
|-------|--------|------|-------|
| admin | admin123 | Администратор | - |
| head_it | pass123 | Руководитель | IT |
| head_hr | pass123 | Руководитель | HR |
| dev1 | pass123 | Сотрудник | IT |
| dev2 | pass123 | Сотрудник | IT |
| hr1 | pass123 | Сотрудник | HR |

## 📊 Иерархия и права доступа

Система поддерживает 5 ролей с различными уровнями доступа:

### 1. Администраторы (admin)
- ✅ Полный доступ ко всей системе
- ✅ Видят все чаты и сообщения
- ✅ Могут писать всем в ЛС
- ✅ Могут создавать/удалять пользователей
- ✅ Могут создавать/удалять чаты и отделы
- ✅ Управление ботами и webhooks
- ✅ Доступ ко всей аналитике
- ✅ Управление системой поддержки

### 2. РОП - Руководитель Отдела Производства (rop)
- ✅ Управление своим отделом
- ✅ Могут писать админам и другим РОП в ЛС
- ✅ Могут писать сотрудникам своего отдела
- ✅ Видят чаты своего отдела
- ✅ Могут создавать групповые чаты отдела
- ✅ Доступ к аналитике своего отдела
- ✅ Могут назначать задачи ассистентам
- ❌ Не могут видеть чаты других отделов

### 3. Ассистенты (assistant)
- ✅ Поддержка РОП и операторов
- ✅ Могут писать админам, РОП и операторам
- ✅ Видят чаты своего отдела (Ассистенты)
- ✅ Могут обрабатывать тикеты поддержки
- ✅ Доступ к истории звонков
- ✅ Могут создавать и управлять опросами
- ❌ Ограниченный доступ к аналитике

### 4. Операторы (operator)
- ✅ Работа с клиентами через чаты
- ✅ Могут писать админам и ассистентам
- ✅ Видят чаты своего отдела
- ✅ Могут создавать тикеты поддержки
- ✅ Доступ к видеозвонкам
- ❌ Не могут создавать групповые чаты
- ❌ Ограниченный доступ к настройкам

### 5. Сотрудники (employee)
- ✅ Базовые права для работы в системе
- ✅ Могут писать админам и своему руководителю в ЛС
- ✅ Видят чаты своего отдела
- ✅ Могут участвовать в видеозвонках
- ✅ Могут голосовать в опросах
- ❌ Не могут писать в групповые чаты
- ❌ Не могут писать другим сотрудникам в ЛС
- ❌ Не могут видеть чаты других отделов

> 💡 Права доступа настраиваются в `config/permissionsMatrix.js` и контролируются через middleware `requireAdmin`, `requireRop`, `requireAssistant`, `requireOperator`.

## 🗄️ Структура базы данных

### Основные таблицы

```
users                  - Пользователи
├── id                 - ID (primary key)
├── username           - Логин (unique)
├── password_hash      - Хеш пароля (bcrypt)
├── name               - Полное имя
├── role               - Роль (admin/rop/assistant/operator/employee)
├── department         - Отдел
├── is_active          - Активен ли аккаунт
└── last_seen          - Последняя активность

departments            - Отделы
├── id                 - ID (primary key)
├── name               - Название отдела
├── description        - Описание
└── head_user_id       - ID руководителя отдела

chats                  - Чаты
├── id                 - ID (primary key)
├── name               - Название (для группы/отдела)
├── type               - Тип (direct/group/department)
├── department         - Отдел (для department)
└── created_by         - Кто создал

chat_participants      - Участники чатов
├── chat_id            - ID чата
├── user_id            - ID пользователя
└── last_read_at       - Время последнего прочтения

department_chats       - Связь отделов и чатов
├── department_id      - ID отдела
└── chat_id            - ID чата

messages               - Сообщения
├── id                 - ID (primary key)
├── chat_id            - ID чата
├── user_id            - ID отправителя
├── content            - Содержимое
├── is_edited          - Было ли отредактировано
├── is_deleted         - Удалено ли (soft delete)
├── parent_message_id  - ID родительского сообщения (для пересылки)
└── created_at         - Время отправки

reactions              - Реакции на сообщения
├── id                 - ID (primary key)
├── message_id         - ID сообщения
├── user_id            - ID пользователя
├── emoji              - Emoji реакция
└── created_at         - Время создания

message_forwards       - Пересланные сообщения
├── id                 - ID (primary key)
├── original_message_id - ID оригинального сообщения
├── forwarded_message_id - ID пересланного сообщения
├── forwarded_by       - Кто переслал
└── forwarded_at       - Время пересылки
```

### Support System (Troubleshooting)

```
support_tickets        - Тикеты поддержки
├── id                 - ID (primary key)
├── title              - Заголовок
├── description        - Описание проблемы
├── priority           - Приоритет (low/medium/high/urgent)
├── status             - Статус (open/in_progress/resolved/closed)
├── created_by         - Кто создал
├── assigned_to        - Кому назначен
├── team_id            - ID команды
├── sla_deadline       - Дедлайн по SLA
├── resolved_at        - Время решения
└── created_at         - Время создания

ticket_messages        - Сообщения в тикетах
├── id                 - ID (primary key)
├── ticket_id          - ID тикета
├── user_id            - ID пользователя
├── content            - Содержимое
├── is_internal        - Внутреннее сообщение
└── created_at         - Время создания

ticket_status_history  - История изменений статусов
├── id                 - ID (primary key)
├── ticket_id          - ID тикета
├── old_status         - Старый статус
├── new_status         - Новый статус
├── changed_by         - Кто изменил
├── notes              - Примечания
└── changed_at         - Время изменения

support_teams          - Команды поддержки
├── id                 - ID (primary key)
├── name               - Название команды
├── description        - Описание
└── is_active          - Активна ли команда

support_team_members   - Участники команд
├── team_id            - ID команды
├── user_id            - ID пользователя
├── role               - Роль (agent/lead)
└── joined_at          - Дата присоединения

support_automation_rules - Правила автоматизации
├── id                 - ID (primary key)
├── name               - Название правила
├── trigger_type       - Тип триггера
├── conditions         - Условия (JSON)
├── actions            - Действия (JSON)
└── is_active          - Активно ли правило

support_metrics_reports - Метрики и отчёты
├── id                 - ID (primary key)
├── report_type        - Тип отчёта
├── period_start       - Начало периода
├── period_end         - Конец периода
├── metrics            - Метрики (JSON)
└── generated_at       - Время генерации
```

### Push Notifications

```
push_subscriptions     - Подписки на push
├── id                 - ID (primary key)
├── user_id            - ID пользователя
├── endpoint           - Endpoint для push
├── p256dh_key         - Ключ шифрования
├── auth_key           - Ключ аутентификации
├── last_used_at       - Последнее использование
└── created_at         - Время создания

notification_settings  - Настройки уведомлений
├── id                 - ID (primary key)
├── user_id            - ID пользователя
├── new_messages       - Уведомления о сообщениях
├── mentions           - Уведомления об упоминаниях
├── tickets            - Уведомления о тикетах
└── calls              - Уведомления о звонках

notification_logs      - Логи уведомлений
├── id                 - ID (primary key)
├── user_id            - ID пользователя
├── type               - Тип уведомления
├── title              - Заголовок
├── body               - Содержимое
├── status             - Статус (sent/failed)
└── sent_at            - Время отправки
```

### Video Calls (Jitsi)

```
calls                  - Звонки
├── id                 - ID (primary key)
├── chat_id            - ID чата
├── room_name          - Название комнаты Jitsi
├── initiated_by       - Кто инициировал
├── started_at         - Время начала
├── ended_at           - Время окончания
└── created_at         - Время создания

call_participants      - Участники звонков
├── id                 - ID (primary key)
├── call_id            - ID звонка
├── user_id            - ID пользователя
├── status             - Статус (joined/left/missed)
├── joined_at          - Время присоединения
├── left_at            - Время выхода
└── duration           - Продолжительность (секунды)

call_events            - События во время звонков
├── id                 - ID (primary key)
├── call_id            - ID звонка
├── event_type         - Тип события (mute/unmute/screenshare)
├── user_id            - ID пользователя
└── created_at         - Время события
```

### Bots & Webhooks

```
bots                   - Боты
├── id                 - ID (primary key)
├── name               - Название бота
├── username           - Username бота
├── token              - Токен доступа
├── description        - Описание
├── is_active          - Активен ли бот
└── created_at         - Время создания

webhooks               - Webhooks
├── id                 - ID (primary key)
├── name               - Название webhook
├── url                - URL для отправки
├── events             - События для триггера (JSON array)
├── secret             - Секретный ключ
├── bot_id             - ID бота
├── is_active          - Активен ли webhook
└── created_at         - Время создания
```

### Polls & Other Features

```
polls                  - Опросы
├── id                 - ID (primary key)
├── chat_id            - ID чата
├── question           - Вопрос
├── created_by         - Кто создал
├── expires_at         - Время истечения
└── created_at         - Время создания

poll_options           - Варианты ответов
├── id                 - ID (primary key)
├── poll_id            - ID опроса
├── option_text        - Текст варианта
└── votes_count        - Количество голосов

poll_votes             - Голоса в опросах
├── id                 - ID (primary key)
├── poll_id            - ID опроса
├── option_id          - ID варианта
├── user_id            - ID пользователя
└── voted_at           - Время голосования

registration_requests  - Заявки на регистрацию
├── id                 - ID (primary key)
├── email              - Email
├── name               - Имя
├── department         - Желаемый отдел
├── status             - Статус (pending/approved/rejected)
├── token              - Токен подтверждения
└── created_at         - Время создания

files                  - Файлы
├── id                 - ID (primary key)
├── filename           - Имя файла
├── original_name      - Оригинальное имя
├── mime_type          - MIME тип
├── size               - Размер (байты)
├── uploaded_by        - Кто загрузил
└── uploaded_at        - Время загрузки

file_access_logs       - Логи доступа к файлам
├── id                 - ID (primary key)
├── file_id            - ID файла
├── user_id            - ID пользователя
├── action             - Действие (download/delete)
└── accessed_at        - Время доступа
```

> 📖 **Подробная документация**: Полное описание схемы БД и миграций доступно в [docs/architecture.md](docs/architecture.md)

## 🔒 Безопасность

### Реализованные меры

1. **Аутентификация и авторизация**
   - JWT токены с коротким временем жизни (24ч)
   - Refresh tokens для обновления сессий (7 дней)
   - Проверка прав на каждый запрос
   - Автоматическая деактивация истекших токенов

2. **Защита паролей**
   - Bcrypt хеширование (12 раундов)
   - Никогда не храним пароли открыто
   - Валидация сложности паролей

3. **Защита от атак**
   - Rate limiting (100 запросов / 15 минут)
   - Stricter rate limiting для auth (5 попыток / 15 минут)
   - Helmet для защиты HTTP заголовков
   - CORS правильно настроен
   - Parameterized queries (защита от SQL-инъекций)
   - Валидация и санитизация всех входных данных

4. **Безопасность WebSocket**
   - Аутентификация через JWT при подключении
   - Проверка прав на каждое действие
   - Автоматическое отключение при истечении токена

## 📈 Производительность

### Оптимизации

- ✅ Пул соединений PostgreSQL (max 20)
- ✅ Индексы на всех часто запрашиваемых полях
- ✅ Пагинация всех списков
- ✅ Ленивая загрузка сообщений
- ✅ Эффективные JOIN запросы
- ✅ Кэширование на уровне БД (prepared statements)

### Рекомендации для Production

1. **Добавить Redis** для кэширования часто запрашиваемых данных
2. **Настроить CDN** для статических файлов
3. **Использовать PM2** для управления процессами
4. **Настроить Nginx** как reverse proxy
5. **Мониторинг** через Prometheus + Grafana

## 🚀 Деплой в Production

### 1. Настройка сервера

```bash
# Обновить систему
sudo apt update && sudo apt upgrade -y

# Установить PostgreSQL
sudo apt install postgresql postgresql-contrib

# Установить Node.js (через nvm рекомендуется)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 18
nvm use 18

# Установить PM2 глобально
npm install -g pm2
```

### 2. Деплой приложения

```bash
# Клонировать репозиторий
git clone <your-repo-url>
cd corporate-chat-backend

# Установить зависимости
npm install --production

# Настроить .env для production
nano .env
# Установить NODE_ENV=production
# Использовать сильные секретные ключи
# Настроить правильный DB_HOST, DB_PASSWORD

# Инициализировать БД
psql -U postgres -d corporate_chat -f database/schema.sql
node database/seed.js

# Запустить с PM2
pm2 start server.js --name corporate-chat
pm2 save
pm2 startup
```

### 3. Настройка Nginx

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # WebSocket support
    location /socket.io/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
    }
}
```

### 4. SSL сертификат (Let's Encrypt)

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

## 🧪 Тестирование

Проект включает **30+ автоматических тестов** с использованием Jest.

### Запуск тестов

```bash
# Запустить все тесты
npm test

# Запустить тесты с покрытием
npm run test:coverage

# Запустить конкретный тестовый файл
npm test -- auth.test.js

# Запустить проверку (тесты + линтинг)
./scripts/check_all.sh
```

### Покрытие тестами

- ✅ Аутентификация (JWT, bcrypt, refresh tokens)
- ✅ Управление пользователями (CRUD, роли)
- ✅ Чаты и сообщения (отправка, редактирование, удаление)
- ✅ Права доступа (permissions matrix)
- ✅ WebSocket события
- ✅ Support система (тикеты, команды)
- ✅ Push уведомления
- ✅ Файлы (загрузка, валидация)

### Тестирование API (curl)

```bash
# Проверка health
curl http://localhost:3000/api/health

# Вход в систему
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# Получить чаты (с токеном)
curl http://localhost:3000/api/chats \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

### Тестирование WebSocket

```javascript
const io = require('socket.io-client');

const socket = io('http://localhost:3000', {
    auth: { token: 'YOUR_TOKEN' }
});

socket.on('connect', () => {
    console.log('Connected!');
    socket.emit('send_message', {
        chatId: 1,
        content: 'Test message'
    });
});

socket.on('new_message', (data) => {
    console.log('New message:', data);
});
```

## 📝 Логирование

В development режиме логируются:
- Все запросы к API
- SQL запросы с временем выполнения
- WebSocket события
- Ошибки

В production режиме:
- Только ошибки и важные события
- Рекомендуется настроить Winston или Bunyan

## 🔍 Мониторинг

Рекомендуемый стек для production:

1. **Prometheus** - сбор метрик
2. **Grafana** - визуализация
3. **Sentry** - трекинг ошибок
4. **PM2 Monitoring** - состояние процессов

## 📞 Поддержка и Troubleshooting

При возникновении проблем:

1. **Проверьте документацию**: [docs/runbooks/common-issues.md](docs/runbooks/common-issues.md) содержит решения типичных проблем
2. **Проверьте логи**: `pm2 logs corporate-chat --lines 50`
3. **Проверьте подключение к БД**: `psql -p 5433 -U postgres -d corporate_chat`
4. **Проверьте переменные окружения** в `.env`
5. **Убедитесь, что порт не занят**: `lsof -i :3000`
6. **Запустите тесты**: `npm test`

### Частые проблемы

- **502 Bad Gateway**: Проверьте `pm2 logs corporate-chat --err`, возможно сервер не запустился
- **Logger is not a constructor**: Используйте `createLogger` вместо `new Logger()` (см. docs/runbooks/common-issues.md)
- **404 на существующий endpoint**: Проверьте порядок подключения роутов в server.js
- **Push notifications не работают**: Проверьте VAPID ключи в .env и Service Worker регистрацию

### Автоматическая диагностика

```bash
# Запустить полную проверку
./scripts/check_all.sh

# Просмотреть статус PM2
pm2 status

# Просмотреть использование ресурсов
pm2 monit
```

## 📜 Лицензия

ISC

## 🎉 Готово к Production!

Этот backend полностью готов к развертыванию в продакшен окружении с учетом всех требований безопасности, производительности и масштабируемости.

### Основные возможности

✅ Real-time чаты с Socket.io
✅ Иерархическая система доступа (5 ролей)
✅ Система поддержки (Support/Troubleshooting) с SLA мониторингом
✅ Web Push уведомления
✅ Видеозвонки через Jitsi Meet
✅ Боты и Webhooks для интеграций
✅ Опросы в чатах
✅ Регистрация с email-подтверждением
✅ 30+ автоматических тестов
✅ Полная документация (AI Dev 2025)

### Архитектура

Проект следует **AI Dev 2025 протоколу** с полной документацией:
- 📖 [CLAUDE.md](CLAUDE.md) - протокол разработки
- 📖 [docs/architecture.md](docs/architecture.md) - архитектура системы
- 📖 [docs/ai-coding.md](docs/ai-coding.md) - правила разработки
- 📖 [docs/runbooks/common-issues.md](docs/runbooks/common-issues.md) - troubleshooting

**Готов к работе!** 🚀
