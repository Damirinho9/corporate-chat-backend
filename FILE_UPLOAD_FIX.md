# 📎 Исправление загрузки файлов

## Проблема

Ошибка 500 Internal Server Error при попытке загрузить файл:
```
Failed to load resource: the server responded with a status of 500
/api/files/upload
```

## Причины

1. ❌ Отсутствовали middleware для аутентификации и обработки файлов
2. ❌ Отсутствовала таблица `files` в базе данных
3. ❌ Не были созданы директории для хранения файлов

## Решение

### Шаг 1: Обновите код

```bash
cd ~/corporate-chat-backend
git pull
```

### Шаг 2: Создайте файл .env (если его нет)

```bash
nano .env
```

Добавьте строку с подключением к БД:
```env
DATABASE_URL=postgresql://username:password@localhost:5432/database_name
```

Замените:
- `username` - ваш пользователь PostgreSQL
- `password` - ваш пароль
- `database_name` - имя вашей базы данных

Например:
```env
DATABASE_URL=postgresql://postgres:mypassword@localhost:5432/corporate_chat
```

### Шаг 3: Примените миграцию

```bash
node apply-file-migration.js
```

**Ожидаемый результат:**
```
🚀 Применение миграции для таблицы files...

✅ Подключение к БД установлено

📦 Выполнение миграции...
✅ Миграция успешно применена!

📋 Таблица files содержит колонки:
   - id (integer)
   - filename (character varying)
   - original_filename (character varying)
   - mime_type (character varying)
   - size_bytes (bigint)
   - path (text)
   - thumbnail_path (text)
   - uploaded_by (integer)
   - message_id (integer)
   - file_type (character varying)
   - width (integer)
   - height (integer)
   - scan_status (character varying)
   - created_at (timestamp without time zone)
   - updated_at (timestamp without time zone)

✅ Колонка file_id добавлена в таблицу messages

✨ Готово! Теперь можно загружать файлы.
```

### Шаг 4: Перезапустите сервер

```bash
npm start
```

### Шаг 5: Проверьте загрузку

1. Откройте чат в браузере
2. Нажмите на значок скрепки 📎
3. Выберите файл
4. Файл должен успешно загрузиться

## Проверка директорий

Убедитесь что созданы директории:

```bash
ls -la uploads/
```

Должны быть:
```
drwxr-xr-x 5 root root 4096 Nov  5 06:57 .
drwxr-xr-x 1 root root 4096 Nov  5 06:57 ..
drwxr-xr-x 2 root root 4096 Nov  5 06:57 files
drwxr-xr-x 2 root root 4096 Nov  5 06:57 temp
drwxr-xr-x 2 root root 4096 Nov  5 06:57 thumbnails
```

Если их нет:
```bash
mkdir -p uploads/{temp,files,thumbnails}
```

## Устранение проблем

### Ошибка: Cannot find module 'dotenv'

```bash
npm install
```

### Ошибка: connect ECONNREFUSED 127.0.0.1:5432

**Причина**: PostgreSQL не запущен или неправильный DATABASE_URL

**Решение 1**: Запустите PostgreSQL
```bash
sudo service postgresql start
```

**Решение 2**: Проверьте DATABASE_URL в .env
```bash
cat .env
```

### Ошибка: permission denied

**Причина**: Нет прав на запись в uploads/

**Решение**:
```bash
chmod -R 755 uploads/
```

### Ошибка: table "files" already exists

**Причина**: Миграция уже была применена

**Решение**: Это нормально, можно пропустить этот шаг

## Поддерживаемые типы файлов

### Изображения
- JPEG, JPG, PNG, GIF, WebP, SVG
- Максимум: 5 МБ
- Автоматическое создание миниатюр

### Документы
- PDF, DOC, DOCX, XLS, XLSX, TXT, CSV
- Максимум: 20 МБ

### Видео
- MP4, MPEG, WebM
- Максимум: 10 МБ

### Аудио
- MP3, WAV, WebM, OGG
- Максимум: 10 МБ

## Безопасность

Заблокированные расширения:
- .exe, .bat, .cmd, .com, .pif, .scr, .vbs, .js, .jar

Все файлы автоматически:
- Сканируются на вирусы (magic numbers)
- Переименовываются (timestamp + random)
- Проверяются на размер
- Валидируются по MIME-type

## Тестирование

После применения миграции и перезапуска сервера:

1. **Откройте чат**
2. **Кликните на 📎** (скрепка)
3. **Выберите изображение** (например, screenshot.png)
4. **Отправьте файл**
5. **Проверьте**:
   - Файл появился в чате
   - Можно кликнуть и открыть
   - Миниатюра отображается для изображений

## Логи для диагностики

При ошибке загрузки проверьте логи сервера:

```bash
# В терминале где запущен сервер будет:
File upload error: Error: ...
```

Типичные ошибки:
- `ENOENT: no such file or directory, open 'uploads/temp/...'` → создайте директории
- `relation "files" does not exist` → примените миграцию
- `column "file_id" of relation "messages" does not exist` → примените миграцию

## Что было исправлено

### routes/files.js
```javascript
// Было:
router.post('/upload', fileController.uploadFile);

// Стало:
router.post('/upload', authenticateToken, uploadSingle, validateFile, fileController.uploadFile);
```

### database/migrations/005_create_files_table.sql
- Создана таблица files
- Добавлена колонка file_id в messages
- Добавлены индексы для оптимизации

### Директории
- uploads/temp - временные файлы
- uploads/files - постоянное хранилище
- uploads/thumbnails - миниатюры изображений

## Готово! 🎉

После выполнения всех шагов загрузка файлов должна работать.

Если возникли проблемы - пришлите:
1. Результат `node apply-file-migration.js`
2. Логи сервера при попытке загрузить файл
3. Содержимое `ls -la uploads/`
