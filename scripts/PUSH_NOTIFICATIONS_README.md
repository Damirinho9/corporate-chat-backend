# Web Push Notifications Setup

## Быстрая настройка

### 1. Установить зависимости
```bash
npm install
```

### 2. Сгенерировать VAPID ключи
```bash
npx web-push generate-vapid-keys
```

### 3. Добавить ключи в .env
```env
VAPID_PUBLIC_KEY=your_generated_public_key
VAPID_PRIVATE_KEY=your_generated_private_key
VAPID_EMAIL=mailto:admin@yourdomain.com
```

### 4. Применить миграцию
```bash
psql -U postgres -d corporate_chat -f database/migrations/011_create_push_subscriptions.sql
```

### 5. Перезапустить сервер
```bash
pm2 restart corporate-chat
# или
npm start
```

## Как работает

1. **Пользователь нажимает 🔔** в интерфейсе чата
2. Браузер запрашивает разрешение на уведомления
3. Service Worker регистрируется и создает подписку
4. Подписка сохраняется на сервере
5. При новых сообщениях оффлайн пользователям отправляется push

## API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/push/vapid-public-key` | GET | Получить публичный ключ VAPID |
| `/api/push/subscribe` | POST | Подписаться на уведомления |
| `/api/push/unsubscribe` | POST | Отписаться от уведомлений |
| `/api/push/status` | GET | Статус подписки пользователя |
| `/api/push/test` | POST | Тестовое уведомление |

## Тестирование

1. Откройте чат в браузере
2. Нажмите 🔕 (кнопка станет 🔔)
3. Разрешите уведомления в браузере
4. Откройте второй аккаунт в другом браузере
5. Закройте первый браузер
6. Отправьте сообщение со второго аккаунта
7. Первый пользователь получит push-уведомление

## Поддержка браузеров

- Chrome/Edge: полная поддержка
- Firefox: полная поддержка
- Safari: iOS 16.4+, macOS
- Android Chrome: полная поддержка

## Troubleshooting

### "Push notifications not configured"
- Проверьте VAPID ключи в .env
- Перезапустите сервер

### Уведомления не приходят
- Проверьте разрешения в браузере
- Убедитесь что Service Worker зарегистрирован (F12 → Application → Service Workers)
- Проверьте таблицу push_subscriptions в БД

### 410 Gone errors
Это нормально - означает что подписка устарела. Система автоматически удаляет такие подписки.

## Структура файлов

```
controllers/pushController.js  - API и логика отправки
public/sw.js                   - Service Worker
database/migrations/011_*      - Схема таблицы
```
