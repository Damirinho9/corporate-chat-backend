# 📝 Настройка системы регистрации пользователей

## Обзор

Система регистрации позволяет новым пользователям подавать заявки на создание учётной записи, которые затем подтверждаются администратором через email.

## Процесс регистрации

1. **Пользователь** заполняет форму регистрации на главной странице
2. **Система** отправляет email администратору с данными заявки
3. **Администратор** получает письмо с кнопками "Подтвердить" / "Отклонить"
4. **При подтверждении** создаётся учётная запись пользователя
5. **Пользователь** получает email с логином и паролем для входа

## Настройка Email (Brevo)

### 1. Получение SMTP ключа от Brevo

1. Зарегистрируйтесь на [Brevo.com](https://www.brevo.com) (бесплатный тариф - до 300 писем/день)
2. Перейдите в **Settings → SMTP & API**
3. Создайте новый SMTP ключ
4. Скопируйте ключ (он понадобится для настройки)

### 2. Настройка переменных окружения

Отредактируйте файл `.env` в корне проекта:

```bash
# ==================== EMAIL CONFIGURATION (BREVO) ====================
# SMTP Configuration for Brevo
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=465
SMTP_USER=9b9c23001@smtp-brevo.com  # Ваш SMTP логин из Brevo
SMTP_PASS=YOUR_BREVO_SMTP_KEY_HERE  # ⚠️ ЗАМЕНИТЕ на ваш SMTP ключ
SMTP_FROM=vaitmarket@ya.ru          # Email отправителя

# Admin email для получения уведомлений о регистрациях
ADMIN_EMAIL=magzhanov.damir@mail.ru  # Email администратора

# URL приложения (для ссылок в письмах)
APP_URL=http://localhost:3000        # ⚠️ Замените на ваш URL в продакшене
```

### 3. Важные замены

⚠️ **ОБЯЗАТЕЛЬНО замените следующие значения:**

- `SMTP_PASS` - ваш SMTP ключ от Brevo
- `SMTP_USER` - ваш SMTP логин (если отличается)
- `SMTP_FROM` - email адрес отправителя (должен быть подтверждён в Brevo)
- `ADMIN_EMAIL` - email администратора для получения уведомлений
- `APP_URL` - URL вашего приложения (для ссылок в письмах)

### 4. Проверка в продакшене

Для продакшн окружения:

```bash
# Установите правильный URL приложения
APP_URL=https://yourdomain.com

# Проверьте, что SMTP_FROM подтверждён в Brevo
SMTP_FROM=noreply@yourdomain.com
```

## База данных

### Миграция

Таблица `registration_requests` будет создана автоматически при первом запуске приложения, если она ещё не существует.

Файл миграции: `database/migrations/013_create_registration_requests.sql`

### Структура таблицы

```sql
- id: Уникальный идентификатор заявки
- email: Email пользователя (используется как логин)
- full_name: ФИО пользователя
- username: Автоматически сгенерированный username
- password_hash: Хэш сгенерированного пароля
- generated_password: Сгенерированный пароль (для отправки)
- role: Роль (assistant, rop, operator, employee)
- department: Отдел (обязателен для rop, operator, employee)
- status: Статус заявки (pending, approved, rejected)
- approval_token: Токен для подтверждения
- created_at: Дата создания заявки
```

## API Endpoints

### 1. Отправка заявки

```http
POST /api/registration/submit
Content-Type: application/json

{
  "email": "user@example.com",
  "full_name": "Иванов Иван Иванович",
  "role": "employee",
  "department": "Отдел продаж"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Заявка на регистрацию отправлена. Ожидайте подтверждения администратора.",
  "request": {
    "email": "user@example.com",
    "full_name": "Иванов Иван Иванович",
    "username": "ivanov_i",
    "role": "employee",
    "department": "Отдел продаж"
  }
}
```

### 2. Подтверждение заявки (администратором)

```http
GET /api/registration/approve/{token}
```

Ссылка отправляется администратору на email. При переходе создаёт учётную запись и отправляет пользователю данные для входа.

### 3. Отклонение заявки (администратором)

```http
GET /api/registration/reject/{token}?reason=Причина+отклонения
```

### 4. Получение списка отделов

```http
GET /api/registration/departments
```

**Ответ:**
```json
{
  "departments": ["Отдел продаж", "Отдел маркетинга", "IT отдел"]
}
```

## Email шаблоны

### Уведомление администратору

Отправляется на `ADMIN_EMAIL` при новой заявке:

- Информация о пользователе (ФИО, email, роль, отдел)
- Автоматически сгенерированный username и пароль
- Кнопки "Подтвердить" и "Отклонить"

### Подтверждение пользователю

Отправляется после одобрения заявки:

- Логин (email)
- Пароль
- Ссылка для входа в систему
- Инструкция по смене пароля

### Отклонение заявки

Отправляется при отклонении:

- Уведомление об отклонении
- Причина отклонения (если указана)

## Тестирование

1. Откройте главную страницу приложения
2. Нажмите кнопку **"Зарегистрироваться"**
3. Заполните форму:
   - ФИО: `Тестовый Пользователь`
   - Email: `test@example.com`
   - Роль: `Сотрудник`
   - Отдел: выберите из списка
4. Нажмите **"Отправить заявку"**
5. Проверьте email администратора (`ADMIN_EMAIL`)
6. Нажмите кнопку "Подтвердить" в письме
7. Проверьте email пользователя (`test@example.com`)
8. Войдите в систему с полученными данными

## Troubleshooting

### Email не отправляются

1. Проверьте, что `SMTP_PASS` установлен корректно
2. Проверьте логи сервера на наличие ошибок SMTP
3. Убедитесь, что `SMTP_FROM` подтверждён в Brevo
4. Проверьте квоту отправки в Brevo (300 писем/день на бесплатном тарифе)

### Ошибка "Email already exists"

Пользователь с таким email уже существует в системе. Используйте другой email.

### Ошибка "Pending request exists"

Заявка с этим email уже ожидает рассмотрения. Дождитесь решения администратора.

### Username генерируется некорректно

Проверьте, что ФИО введено на кириллице. Функция `generateUsername()` использует транслитерацию:
- "Иванов Иван" → "ivanovi"
- "Петров Пётр" → "petrovp"

## Безопасность

1. **Пароли** генерируются автоматически (12 символов, буквы + цифры + спецсимволы)
2. **Токены подтверждения** уникальны и одноразовые (32 байта)
3. **Email validation** проверяет корректность email адреса
4. **Роль assistant** не требует указания отдела
5. **Роли rop, operator, employee** требуют указания отдела

## Поддержка

При возникновении проблем:

1. Проверьте логи сервера: `pm2 logs` или `npm run dev`
2. Проверьте файл `.env` на корректность настроек
3. Убедитесь, что база данных запущена
4. Проверьте таблицу `registration_requests` на наличие записей

---

**Версия:** v2.1.0-registration
**Дата:** Декабрь 2025
